SELECT CLIENTES.NOMBRE, CLIENTES.ID_CLIENTE, PEDIDOS.ID_CLIENTE
FROM CLIENTES
INNER JOIN PEDIDOS
ON CLIENTES.ID_CLIENTE = PEDIDOS.ID_CLIENTE
WHERE DATEDIFF(YEAR, CLIENTES.FECHA_NAC, CURRENT_DATE) > 23
AND DATEDIFF(YEAR, PEDIDOS.FECHA, CURRENT_DATE) <= 5;