SELECT
    LIBROS.ID_LIBRO,
    LIBROS.TITULO,
    PEDIDOS.ID_PEDIDO,
    PEDIDOS.IMPORTE,
    COALESCE(PEDIDOS.GASTOS_ENVIO, 0)
    FROM LIBROS
    INNER JOIN PEDIDOS
    ON LIBROS.ID_LIBRO = PEDIDOS.ID_LIBRO;